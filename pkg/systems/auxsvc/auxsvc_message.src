use uo;
use os;

program auxsMessage(ev)
   if (typeof(ev) != "Struct")
      return;
   endif

   var connection := GetGlobalProperty("aux_service");
   if (connection)
      connection.transmit(json_format);
   endif
endprogram
