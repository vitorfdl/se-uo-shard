use uo;
include ":attributes:attributes";

program fixTalent(who)
   if (HaveFeat(who, "Segredo das Fechaduras"))
      removeFeat(who, "Segredo das Fechaduras")
      who.SetTalentPoints(1);
   endif
endprogram