use os;

include "include/say";
include ":timedscripts:timedScripts";

program magiaSilenciosa(param)
   var who := param[1];
   TS_StartCooldown(who, "amordassombras", 10);
   SendSysMessageEx(who, "As sombras lhe acolhem enquanto estiver próximo a árvores e construções", SSM_INFO);
   return 1;
endprogram
