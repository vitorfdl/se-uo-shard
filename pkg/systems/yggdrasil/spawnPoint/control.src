use uo;
use os;

include ":yggdrasil:spawnsInternal";

program Install(spawnpoint)
   while(spawnpoint)
      start_script(":yggdrasil:spawnpoint/dospawnqueue", spawnpoint);
      sleep(60);
   endwhile
endprogram
