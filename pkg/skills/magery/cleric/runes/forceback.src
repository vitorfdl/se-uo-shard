use uo;
use os;

include "include/utils";
include "include/sysEvent";

program ForceBack(mobile)
   if (!mobile.my_template() || !mobile.my_template()["moriquendi"])
      return;
   endif

   PushBack(mobile, 5, 300);
   var ev := struct{
		type := EVID_PEACEMADE
   };
   SendEvent(mobile, ev);
endprogram