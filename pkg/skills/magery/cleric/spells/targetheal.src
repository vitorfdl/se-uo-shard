use uo;
use os;

include "include/say";

program targetHeal(info)
   var mobile := info[1];
   var targ := info[3];
   info := 0;

   SetObjProperty(mobile, "cleric_spell", targ.serial);
   SendSystemReport(mobile, "Alvo selecionado para benção de cura.");
   SendSystemReport(targ, "Recebendo cura de "+mobile.name);
   return 1;
endprogram