use uo;
use os;

include "include/say";
include ":magery:tnmagery";

program targetHeal(info)
   var mobile := info[1];
   var targ := info[3];
   info := 0;

   SetClericHeal(mobile, targ);
   SendSysMessageEx(mobile, "Alvo selecionado para benção de cura.", SSM_INFO);
   return 1;
endprogram
