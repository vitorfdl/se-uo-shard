use uo;

include ":gumps:gumps";
include ":gumps:gumps_ex";
include ":gumps:gump_prompts";

program PromptDemo(who, text)
	if ( !text )
		text := "Hey, you didn't enter any text to go with the command!";
	endif
	
	GumpPromptMultiLine(who, text);
endprogram
