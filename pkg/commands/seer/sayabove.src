use os;
use uo;
include "include/say";
program textcmd_sayabove( who, text )
    SendSysMessage( who, "Say above what or whom?" );

    var what := Target( who );
    if (what)
        PrintText( what, text );
    endif
endprogram