use uo;
include "include/say";
program textcmd_sayabovepvt( who, text )
    SendSysMessage( who, "Say above what or whom?" );

    var what := Target( who );
    if (what)
        PrintTextPrivate( what, text, what );
		PrintTextPrivate( what, text, who );
    endif
endprogram