use os;

var script := start_script("estalagem/estalagemcontrol");
var script2 := start_script("estalagem/messagercontrol");
if ( script.errortext )
	SysLog(script.errortext);
else
	Print("Starting check-innkeeper script... ok.");
endif
