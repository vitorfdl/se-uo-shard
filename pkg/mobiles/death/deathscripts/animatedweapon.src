

use uo;
use os;
use util;

include ":death:death";

CONST MAX_RANGE	:= 8;

program DeathScript(params)
	var corpse := params[1];
	//var npc_cfg := params[2];
	params := 0; // Not needed anymore

   PrintTextAbove(corpse, "*a arma para de semover*");

	return 1;
endprogram
