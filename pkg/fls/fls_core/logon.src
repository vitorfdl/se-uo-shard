use uo;
use os;

program SendTips(who)
   start_script(":fls_core:tn/character_control", who);
   start_script(":roleplay_window:controller", who);
   start_script(":roleplay_window:window", who);
endprogram

